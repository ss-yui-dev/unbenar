country_decisions = {
	krak_reveal_all_missions = {	
		potential = {
		AND = {
			tag = I02
			NOT = {has_country_flag = krak_missions_part2}
			NOT = {has_country_flag = krak_missions_part3}
			}
		}
		
		allow = {
		}
	
		effect = {
		custom_tooltip = krak_reveal_all_missions_tooltip
		hidden_effect = {
			set_country_flag = krak_missions_part2
			set_country_flag = krak_missions_part3
			swap_non_generic_missions = yes
			}
		}
		ai_will_do = {
			factor = 0
		}
	}
}